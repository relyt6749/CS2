public class Borders
{
   //if (this.getLat().compareTo(other.getLat()) != 0 || this.getLong().compareTo(other.getLong()) != 0)
      //not bordering countries
   //else
      //boardering countries
}